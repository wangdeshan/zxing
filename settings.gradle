pluginManagement {
    repositories {
        google {}
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google {}
        mavenCentral()
        maven {
            url = 'https://jitpack.io'
            content {
                includeGroupByRegex("com\\.github.*")
            }
        }
    }
}
include ':app', ":core"
